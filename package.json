{
  "name": "contalink-test-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright + TypeScript automation suite for Contalink candidates app",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test tests/e2e",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "dotenv": "^16.4.5",
    "typescript": "^5.6.3"
  }
}
